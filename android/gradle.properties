org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental build-tools-API cache fails on Windows when the Gradle
# project and a dependency (e.g. the pub cache) live on different drive
# letters ("this and base files have different roots"). Disable incremental
# Kotlin compilation to avoid that path-relativization bug.
kotlin.incremental=false
